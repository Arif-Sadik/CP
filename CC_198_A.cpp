#include<bits/stdc++.h>
#define ll long long
#define dubb long double
#define pi acos(-1)
using namespace std;

int main()
{
    ll n,k,x, c, s, d, t;
    string str;
    cin>>str;
    sort(str.begin(), str.end());

    if (str=="act")
    {
        cout<<"yEs";
    }
    else cout<<"nO";

    return 0;
}